---
phase: 06-promotion-integrity-diagnostics
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/invariants.md
autonomous: true
requirements: [INV-04, INV-05]

must_haves:
  truths:
    - "docs/invariants.md contains an INV-04 section documenting the rule that deployed image tag must equal build SHA"
    - "INV-04 section names the verify-promotion CI job as its enforcement mechanism"
    - "docs/invariants.md contains an INV-05 section documenting the production smoke test prerequisite rule"
    - "INV-05 section contains the note about Phase 07 planned enforcement"
    - "docs/invariants.md enforcement table has a new per-push row for the verify-promotion job"
  artifacts:
    - path: "docs/invariants.md"
      provides: "INV-04 and INV-05 invariant documentation with enforcement references and updated table"
      contains: "INV-04"
  key_links:
    - from: "INV-04"
      to: "verify-promotion job"
      via: "Enforced by section"
      pattern: "verify-promotion"
    - from: "INV-05"
      to: "Phase 07 enforcement plan"
      via: "planned enforced note"
      pattern: "planned: enforced by Phase 07"
---

<objective>
Append INV-04 and INV-05 to `docs/invariants.md` and add a per-push row to the enforcement table.

Purpose: INV-04 formally documents that the verify-promotion job enforces same-tag promotion. INV-05 documents the production smoke test requirement, noting that machine enforcement lands in Phase 07. The enforcement table gains a "per-push" layer alongside the existing check/enforce/monitor layers.

Output: `docs/invariants.md` with two new invariant sections (following INV-01 format) and an updated enforcement table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-promotion-integrity-diagnostics/06-CONTEXT.md

<interfaces>
<!-- INV-01 format from docs/invariants.md — use this as the structural template for INV-04 and INV-05 -->
<!--
Each invariant section follows this structure:
  ## INV-NN: [title]

  **Rule**: [what must always be true, one or two sentences]

  **Why this matters**: [consequence of violation, 2-4 sentences]

  **Enforced by**:
  - [job/tool] → [what it checks, what happens on failure]

  **To remediate manually**:
  1. [step]
  2. [step]

  **Not affected by**: [list of things that do not trigger this invariant]

Each section is preceded by a horizontal-rule line (`---` on its own line, matching the
separator pattern used between INV-01, INV-02, and INV-03 in the existing file).

Existing enforcement table (lines 8-13 of docs/invariants.md):
  | Layer | Tool | Behavior |
  |-------|------|----------|
  | **Check (advisory)** | `validate.sh` | Warns about violations — exits 0 so provision can fix them |
  | **Enforce (fix)** | `provision.sh` | Removes or repairs violations on every run |
  | **Monitor (continuous)** | `deploy.yml` drift-check job | Fails the weekly CI job if a violation is detected in a live app |

New row to append immediately after the Monitor row:
  | **Assert (per-push)** | `deploy.yml` verify-promotion job | Fails CI if staging ≠ production image tag |
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append INV-04 and INV-05 sections and update enforcement table (INV-04, INV-05)</name>
  <files>docs/invariants.md</files>

  <read_first>
  - docs/invariants.md — read the entire file; confirm the enforcement table location (lines 8-13)
    and locate the "Adding a new invariant" heading that must remain last after all edits
  </read_first>

  <action>
Make two edits to `docs/invariants.md`.

**Edit 1 — Add per-push row to the enforcement table:**

Locate the table row that reads:
  | **Monitor (continuous)** | `deploy.yml` drift-check job | Fails the weekly CI job if a violation is detected in a live app |

Insert the following row immediately after it (before the blank line that follows the table):
  | **Assert (per-push)** | `deploy.yml` verify-promotion job | Fails CI if staging ≠ production image tag |

**Edit 2 — Insert INV-04 and INV-05 sections before "Adding a new invariant":**

Locate the `## Adding a new invariant` heading at the bottom of the file. Insert both new invariant
sections immediately before it. Each section is preceded by a horizontal rule line (`---` on its own
line) and a blank line after it, exactly matching the separator style used between INV-01, INV-02, and INV-03.

The INV-04 section content (with the horizontal rule before it):

  A horizontal rule line (`---`), then a blank line, then:

  ## INV-04: Deployed image tag must equal the build SHA on every Coolify app

  **Rule**: Both staging and production Coolify applications must have `docker_registry_image_tag`
  set to the commit short SHA (`${GITHUB_SHA:0:7}`) produced by the current build job.
  No app may hold a tag that differs from the one built and promoted in the same workflow run.

  **Why this matters**: If either app's tag drifts from the build SHA — due to a manual update in
  the Coolify UI, a failed PATCH step, or a partial workflow run — the same-image promotion guarantee
  is broken silently. The operator would believe staging and production are on the same image when they
  are not. This class of failure is hard to diagnose because Coolify shows what tag it was told to use,
  not what is actually running in the container.

  **Enforced by**:
  - `deploy.yml` verify-promotion job → runs after both deploy-staging and deploy-production complete;
    queries Coolify `GET /api/v1/applications/{uuid}` for each app; compares `docker_registry_image_tag`
    against the build job's `$TAG` output; exits 1 with `FAIL verify-promotion: diverged tags detected`
    if either app does not match. Blocks ghcr-cleanup from running, preserving all GHCR tags for debugging.

  **To remediate manually**:
  1. Check the "Set image tag" step logs in the failed workflow run for Coolify API errors
  2. Re-run the failed workflow from the GitHub Actions tab — the PATCH step is idempotent
  3. Or set the tag manually in the Coolify UI: Application → Image → Tag field

  **Not affected by**: Doppler secret changes, application config updates, drift-check schedule runs,
  or redeployments that do not change the tag value.

Then another horizontal rule line (`---`), a blank line, then the INV-05 section:

  ## INV-05: Production smoke test must pass before the workflow completes

  **Rule**: The workflow must not reach the `ghcr-cleanup` step unless the production deployment
  has been verified as healthy by a smoke test. A failing production smoke test must block cleanup
  so that GHCR tags are preserved for diagnosing and rolling back the failure.

  **Why this matters**: Running GHCR cleanup after a failed production deploy removes the image tags
  needed to diagnose or roll back. Retaining tags gives operators a full history of what ran and
  allows redeployment of the last known-good tag without rebuilding. Cleanup is a permanent action —
  once old tags are deleted they cannot be recovered from GHCR without a new build.

  **Enforced by**:
  - (planned: enforced by Phase 07 — production smoke test job added to deploy.yml)

  **To remediate manually**:
  1. Check the production deploy job logs in the Actions run for the failure cause
  2. If the image is at fault, identify the last known-good short SHA from prior workflow runs
  3. Manually set `docker_registry_image_tag` to the known-good SHA in Coolify UI and redeploy

  **Not affected by**: Staging smoke test results, infrastructure changes, Doppler secret rotations,
  or verify-promotion assertion status.

Then a final horizontal rule line (`---`), a blank line, then the existing `## Adding a new invariant`
section follows (it was already there — do not duplicate it).

**Notes:**
- `${GITHUB_SHA:0:7}` in the INV-04 Rule is documentation prose; write it literally with backticks
- The `(planned: enforced by Phase 07 — production smoke test job added to deploy.yml)` line is the
  complete content of INV-05's Enforced-by bullet — no other enforcement exists yet
- After all edits, `## Adding a new invariant` must remain the last `##` heading in the file
  </action>

  <verify>
    <automated>
grep -c 'INV-04' /home/cnut/development/claude-skills-deploy/docs/invariants.md
grep -c 'INV-05' /home/cnut/development/claude-skills-deploy/docs/invariants.md
grep -c 'verify-promotion' /home/cnut/development/claude-skills-deploy/docs/invariants.md
grep 'planned: enforced by Phase 07' /home/cnut/development/claude-skills-deploy/docs/invariants.md
grep 'Assert (per-push)' /home/cnut/development/claude-skills-deploy/docs/invariants.md
grep -n 'Adding a new invariant' /home/cnut/development/claude-skills-deploy/docs/invariants.md
    </automated>
  </verify>

  <acceptance_criteria>
  - `grep -c 'INV-04' docs/invariants.md` outputs at least 3 (heading, rule text, enforced-by reference)
  - `grep -c 'INV-05' docs/invariants.md` outputs at least 3 (heading, rule text, enforced-by reference)
  - `grep 'verify-promotion' docs/invariants.md` shows at least 2 lines (enforcement table row + INV-04 enforced-by text)
  - `grep 'planned: enforced by Phase 07' docs/invariants.md` shows exactly 1 match inside the INV-05 section
  - `grep 'Assert (per-push)' docs/invariants.md` shows the new enforcement table row
  - `grep -n 'Adding a new invariant' docs/invariants.md` returns the highest-numbered `##` heading line (confirmed by the section being last)
  - `grep 'docker_registry_image_tag' docs/invariants.md` shows INV-04 Enforced-by text referencing the Coolify API field
  - INV-04 Enforced-by names the `verify-promotion job` explicitly
  - INV-05 Enforced-by contains the text `planned: enforced by Phase 07`
  </acceptance_criteria>

  <done>docs/invariants.md has INV-04 and INV-05 sections following INV-01 format; the enforcement table has a new per-push row for verify-promotion; "Adding a new invariant" remains the last section</done>
</task>

</tasks>

<verification>
```bash
# Content checks
grep -c 'INV-04' docs/invariants.md             # at least 3
grep -c 'INV-05' docs/invariants.md             # at least 3
grep 'Assert (per-push)' docs/invariants.md     # shows table row
grep 'planned: enforced by Phase 07' docs/invariants.md   # 1 match in INV-05

# Structure: Adding a new invariant must be last heading
tail -20 docs/invariants.md | grep 'Adding a new invariant'
```
</verification>

<success_criteria>
- docs/invariants.md enforcement table has 4 rows (check, enforce, monitor, assert per-push)
- INV-04 section present with rule, why, enforced-by (verify-promotion), remediate, not-affected-by fields
- INV-05 section present with rule, why, enforced-by (planned Phase 07), remediate, not-affected-by fields
- Both sections use INV-01 heading and field structure
- "Adding a new invariant" section remains last
- INV-04 and INV-05 requirements fully addressed
</success_criteria>

<output>
After completion, create `.planning/phases/06-promotion-integrity-diagnostics/06-02-SUMMARY.md`
</output>

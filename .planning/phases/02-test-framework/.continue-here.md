---
phase: 02-test-framework
task: UAT-01 (live E2E run)
total_tasks: 2
status: in_progress
last_updated: 2026-05-22T17:44:19.461Z
---

<current_state>
Phase 2 plans are fully executed and verified (7/7 automated checks pass). We are in
the human UAT phase trying to validate TEST-01 (live E2E run). The test runs 9/10 steps
successfully but the Coolify container deploy fails at Step 7 in ~11 seconds.

A live test environment is currently preserved with --keep:
  Coolify project: csd-e2e-20260522104146 (uuid: o3959oqvsowxk9b0bz5p7mum)
  Staging app UUID: eivrumxrijou619cvrzolukn
  App status: exited:unhealthy
  Deployment UUID: syfmc0nf4q0r2zier0a4z6zb

THE ROOT CAUSE HAS NOT BEEN IDENTIFIED YET.
Most likely candidates (in priority order):
  1. Coolify health check is failing — the nginx container starts but /api/health returns
     non-200 or times out before Coolify marks it healthy
  2. Doppler CLI is not in the hello-world image — but this shouldn't cause startup failure
     since nginx ignores unknown env vars
  3. The image is private and Coolify's Docker API context doesn't use /root/.docker/config.json

WHAT TO DO FIRST: Check the Coolify dashboard at https://coolify.cicd.streamlinity.com
for the deployment logs of the PRESERVED app (eivrumxrijou619cvrzolukn). The dashboard
shows the full Docker deployment log including container startup output.
</current_state>

<completed_work>

## Phase 2 Plans — DONE
- 02-01: test/e2e.sh hardened (E2E_SERVER/E2E_BASE_DOMAIN env vars, conditional teardown
  skip on success, JSON report to test/results/, completion summary block)
- 02-02: test/validate-workflow.sh created (VALID-01 YAML syntax, VALID-02 needs reference resolution)

## Bug Fixes Applied This Session
- `${STAGING_DOMAIN:-}` guard in write_report() — unbound variable on early exit (ada4230)
- Doppler config names changed stg/prd instead of staging/production in e2e.sh (b9e2eda)
- Test image renamed from nested path to flat: ghcr.io/anatesan-stream/csd-hello-world:latest (b9e2eda)

## Infrastructure Setup
- Doppler project `claude-skills-deploy` created with stg/prd configs
- GHCR_TOKEN stored in Doppler claude-skills-deploy/stg (operator stored it)
- Image pushed to ghcr.io/anatesan-stream/csd-hello-world:latest (private)
- VPS GHCR auth configured: root@v_cicd_stream has ghcr.io credentials in /root/.docker/config.json
- .github/workflows/push-test-image.yml added (zero-PAT forker path)

## Docs Updated
- README.md — E2E section reflects Phase 2 behaviour (no-teardown on success, JSON report, env vars)
- docs/setup-guide.md — Step 6b added (GHCR_TOKEN in Doppler), smoke test URL corrected to /api/health
- .planning/PROJECT.md — Phase 2 decisions logged, Active requirements updated

## Verification
- VERIFICATION.md: status=human_needed, score=7/7
- HUMAN-UAT.md created and committed (b8625a4)
</completed_work>

<remaining_work>

## UAT-01: Full E2E success run (TEST-01) — BLOCKED on deploy failure
Fix the Coolify container deploy failure (Step 7), then re-run bash test/e2e.sh and confirm:
  - All 10 steps pass
  - Staging + production apps remain running after exit 0
  - test/results/YYYYMMDD-HHMMSS.json written with 7 fields
  - Deployment complete banner printed

## UAT-02: E2E_SERVER/E2E_BASE_DOMAIN override (TEST-04) — DEFERRED
Requires a second server alias. Can be verified statically (already done) or deferred
to when a second server is available. Static verification already passed.

## After UAT passes
1. Run phase completion: `node gsd-tools.cjs phase complete 02`
2. Update PROJECT.md
3. Commit: "docs(phase-02): complete phase execution"
4. Then proceed to Phase 3 (cleanup-deployment.sh)

## Leftover manual cleanup needed
Coolify project o3959oqvsowxk9b0bz5p7mum was not cleaned up (--keep run).
Run: bash test/e2e.sh (next run will create fresh project; old one stays until manual deletion)
Or: source scripts/lib-coolify-api.sh && coolify_load_server vultr-stream && coolify_curl DELETE "/projects/o3959oqvsowxk9b0bz5p7mum"
Also: doppler projects delete csd-e2e-20260522104146 --yes
</remaining_work>

<decisions_made>

- GHCR_TOKEN stored in Doppler claude-skills-deploy/stg (not env var) — any operator with
  Doppler access can push test image without out-of-band secret sharing
- Test image renamed to flat GHCR path (csd-hello-world) — nested paths (with /) can't be
  made public via GitHub API PATCH endpoint
- Doppler project configs are stg/prd (not staging/production) — Doppler default template
  creates stg/prd; attempting to create staging/production environments fails silently
- VPS GHCR auth via docker login as root — GHCR image is private; Coolify uses the
  Docker daemon's root credentials for image pulls
- workflow_dispatch CI workflow added — zero-PAT path for forkers using GITHUB_TOKEN
</decisions_made>

<blockers>

- BLOCKER: Coolify deploy fails at Step 7 in ~11 seconds, app exits as unhealthy
  - Preserved environment: staging app eivrumxrijou619cvrzolukn
  - Diagnosis: unknown — Coolify logs via SSH and API did not show the failure reason
  - NEXT STEP: Open Coolify dashboard, navigate to the preserved app, view deployment logs
  - URL: https://coolify.cicd.streamlinity.com
  - Most likely fix: check test/hello-world/nginx.conf port and health check path match
    what provision.sh configures (port 3000, /api/health)

- DEFERRED: GHCR package visibility can't be set public via API for nested image names;
  flat name (csd-hello-world) also 404s on PATCH. VPS Docker login is the workaround.
  Long-term: make image public via GitHub UI or rely on VPS Docker credentials.
</blockers>

<context>
Phase 2 is essentially done — all code is correct, all automated checks pass. The only
remaining work is getting the live E2E run to succeed so TEST-01 can be checked off and
the phase marked complete. The deploy failure is the last obstacle.

The hello-world image is nginx:alpine listening on port 3000 serving /api/health → 200.
provision.sh sets health_check_path=/api/health and health_check_port=3000. This should
be correct. The failure at ~11 seconds is fast enough that it's likely a startup issue,
not a health check timeout. Check the Coolify dashboard deployment logs first.
</context>

<next_action>
1. Open https://coolify.cicd.streamlinity.com, find the preserved app
   csd-e2e-20260522104146-staging (app UUID eivrumxrijou619cvrzolukn), open deployment
   syfmc0nf4q0r2zier0a4z6zb and read the deployment logs.
2. Fix whatever is causing the container to exit unhealthy.
3. Re-run: bash test/e2e.sh
4. When 10/10 pass: mark phase 2 complete, proceed to Phase 3.
</next_action>

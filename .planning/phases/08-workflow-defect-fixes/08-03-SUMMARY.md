---
phase: 08-workflow-defect-fixes
plan: "03"
subsystem: docs
tags: [documentation, debt-closure, invariants]
dependency_graph:
  requires: [07-runtime-identity]
  provides: [INV-05-enforced-by-updated]
  affects: [docs/invariants.md]
tech_stack:
  added: []
  patterns: []
key_files:
  created: []
  modified:
    - docs/invariants.md
decisions:
  - "Replace stale (planned:) marker with concrete references to the two steps generated by generate-workflow.sh: Smoke test production and Assert production version"
metrics:
  duration: "2m"
  completed: "2026-06-16T23:59:33Z"
  tasks_completed: 1
  tasks_total: 1
---

# Phase 08 Plan 03: Update INV-05 Enforced-by Section Summary

**One-liner:** Replaced stale Phase 07 planning placeholder in INV-05 with concrete references to the implemented `Smoke test production` and `Assert production version` steps in `deploy-production` job.

## Objective

Close DEBT-2: `docs/invariants.md` INV-05 "Enforced by" section still read `(planned: enforced by Phase 07 — production smoke test job added to deploy.yml)`. Phase 07 is complete; the actual enforcement steps now exist in the generated workflow.

## Tasks Completed

| Task | Description | Commit | Files |
|------|-------------|--------|-------|
| 1 | Update INV-05 Enforced-by bullet | fbcfeb6 | docs/invariants.md |

## Verification

```
grep -c 'Smoke test production' docs/invariants.md  → 1
grep -c 'Assert production version' docs/invariants.md → 1
grep '(planned:' docs/invariants.md → 0 matches
```

All acceptance criteria met.

## Deviations from Plan

**[Rule 3 - Blocking] Merged current docs/invariants.md from main into worktree**
- **Found during:** Task 1 setup
- **Issue:** Worktree branched from `ba589e4` (pre-Phase-06/07); INV-04 and INV-05 not present in worktree's copy of docs/invariants.md.
- **Fix:** `git checkout main -- docs/invariants.md` to obtain the current file with INV-05 stale marker before applying the targeted edit.
- **Files modified:** docs/invariants.md
- **Commit:** fbcfeb6 (combined with the task fix in one atomic commit)

## Known Stubs

None.

## Self-Check: PASSED

- `docs/invariants.md` modified: FOUND (fbcfeb6)
- `Smoke test production` matches: 1
- `Assert production version` matches: 1
- `(planned:` matches: 0

---
phase: 08-workflow-defect-fixes
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/invariants.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "INV-05 Enforced-by bullet references actual implemented steps (Smoke test production, Assert production version)"
    - "No '(planned:' marker remains in docs/invariants.md"
  artifacts:
    - path: "docs/invariants.md"
      provides: "Updated INV-05 Enforced-by section referencing Phase 07 implemented steps"
      contains: "Smoke test production"
gap_closure: true
---

<objective>
Fix DEBT-2: `docs/invariants.md` INV-05 "Enforced by" section (line 127) still reads:
  `- (planned: enforced by Phase 07 — production smoke test job added to deploy.yml)`

Phase 07 is complete. The `Smoke test production` and `Assert production version` steps now exist in the generated `deploy-production` job. Update the Enforced-by bullet to reference the actual implemented steps.

This is a documentation-only fix — no code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/v1.1-MILESTONE-AUDIT.md

<interfaces>
<!-- Current state docs/invariants.md lines 125-128: -->
<!--                                                   -->
<!-- **Enforced by**:                                  -->
<!-- - (planned: enforced by Phase 07 — production smoke test job added to deploy.yml)  -->
<!--                                                   -->
<!-- Required state:                                   -->
<!-- **Enforced by**:                                  -->
<!-- - `Smoke test production` step in `deploy-production` job (generated by `scripts/generate-workflow.sh`) -->
<!-- - `Assert production version` step in `deploy-production` job (gracefully skips if app has no `version` health field) -->

The surrounding context in INV-05 (lines 115-135) describes the invariant that
production smoke test must pass before the workflow completes and GHCR cleanup runs.
The Enforced-by section should reference the concrete steps that implement this.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update INV-05 Enforced-by bullet in docs/invariants.md</name>
  <files>docs/invariants.md</files>
  <read_first>
    - docs/invariants.md lines 115-140 (INV-05 section and Enforced-by bullet)
  </read_first>
  <action>
Make one surgical edit to `docs/invariants.md` at the INV-05 Enforced-by section.

Old text (line 127):
```
- (planned: enforced by Phase 07 — production smoke test job added to deploy.yml)
```

New text (replace that single line with two lines):
```
- `Smoke test production` step in `deploy-production` job (generated by `scripts/generate-workflow.sh`)
- `Assert production version` step in `deploy-production` job (gracefully skips if app has no `version` health field)
```

Do not touch any other lines in the file. This changes a stale planning note into a reference to the actual implemented enforcement mechanism.
  </action>
  <verify>
    <automated>grep -c 'Smoke test production' docs/invariants.md && grep -c 'Assert production version' docs/invariants.md && ! grep -n '(planned:' docs/invariants.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Smoke test production' docs/invariants.md` returns 1
    - `grep -c 'Assert production version' docs/invariants.md` returns 1
    - `grep '(planned:' docs/invariants.md` returns 0 lines (stale marker removed)
    - The INV-05 section still has the `**Enforced by**:` heading immediately above the new bullets
  </acceptance_criteria>
  <done>INV-05 Enforced-by now references the actual implemented steps rather than a planning placeholder</done>
</task>

</tasks>

<verification>
- `grep 'Smoke test production' docs/invariants.md` → 1 match
- `grep '(planned:' docs/invariants.md` → 0 matches
- `grep 'Assert production version' docs/invariants.md` → 1 match
</verification>

<success_criteria>
DEBT-2 closed. docs/invariants.md INV-05 Enforced-by section accurately reflects the Phase 07 implementation. No stale planning notes remain.
</success_criteria>

<output>
After completion, create `.planning/phases/08-workflow-defect-fixes/08-03-SUMMARY.md`
</output>

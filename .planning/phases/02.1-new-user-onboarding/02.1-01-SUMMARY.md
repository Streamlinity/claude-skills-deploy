---
phase: 02.1-new-user-onboarding
plan: 01
subsystem: testing
tags: [bash, e2e, onboarding, ux]

# Dependency graph
requires: []
provides:
  - "test/e2e.sh fails fast with actionable errors when E2E_SERVER or E2E_BASE_DOMAIN are unset"
  - "E2E_IMAGE documented with origin, sentinel string, and custom-image path"
affects: [02.1-02, 02.1-03, 02.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Missing-var guard pattern: accumulate all errors before exit 1, each error names the var and points to the fix"

key-files:
  created: []
  modified:
    - test/e2e.sh

key-decisions:
  - "Accumulate both missing-var errors before exiting (collect all, report all, exit 1) — matches validate.sh convention"
  - "E2E_IMAGE retains its public default (domain-neutral image) — only adds explanatory comment"
  - "--server flag continues to satisfy E2E_SERVER requirement; guard skips E2E_SERVER check when SERVER_ALIAS already set"

patterns-established:
  - "Missing-var guard: MISSING_VARS array accumulates failures, case statement emits per-var message, single exit 1 at end"

requirements-completed: [ONBOARD-01, ONBOARD-02]

# Metrics
duration: 2min
completed: 2026-05-22
---

# Phase 02.1 Plan 01: Remove Silent E2E Defaults Summary

**E2E_SERVER and E2E_BASE_DOMAIN now fail fast with actionable errors pointing to /setup-coolify init, replacing silent vultr-stream/cicd.streamlinity.com defaults**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-22T19:26:27Z
- **Completed:** 2026-05-22T19:28:43Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Removed `vultr-stream` and `cicd.streamlinity.com` maintainer-specific defaults from `test/e2e.sh`
- Added guard block that accumulates both missing-var errors and exits 1 with clear actionable messages
- `--server` flag correctly satisfies the E2E_SERVER requirement (guard skips when `SERVER_ALIAS` already set)
- Added multi-line explanatory comment above `E2E_IMAGE` documenting origin, sentinel string, and path to build a custom image

## Task Commits

1. **Task 1: Replace E2E_SERVER / E2E_BASE_DOMAIN silent defaults with explicit missing-var guards** - `5a706fd` (fix)
2. **Task 2: Add explanatory comment block above E2E_IMAGE** - `8c08ba1` (docs)

**Plan metadata:** (pending final commit)

## Files Created/Modified
- `test/e2e.sh` - Replaced silent defaults with guard block; added E2E_IMAGE comment block

## Decisions Made
- Accumulated both missing-var errors before exiting (matches validate.sh pattern; context D-03)
- E2E_IMAGE default value unchanged — image is public and domain-neutral; only documentation improved
- Guard placed after argument-parsing loop so `--server` flag can pre-populate `SERVER_ALIAS` before the guard runs

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Grep acceptance criteria in the plan use `grep -q 'E2E_SERVER="${E2E_SERVER:-}"'` but without `-F` the `$` is treated as a regex anchor by grep. This does not affect correctness — the file content is verified with `-F` flag and matches exactly. The plan's grep check is an artifact of bash quoting in regex mode.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plan 01 complete: `test/e2e.sh` no longer silently defaults to maintainer infrastructure
- Ready for Plan 02: SKILL.md accuracy fixes (stale function references and broken See also link)

---
*Phase: 02.1-new-user-onboarding*
*Completed: 2026-05-22*

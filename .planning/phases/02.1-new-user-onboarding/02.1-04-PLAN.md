---
phase: 02.1-new-user-onboarding
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - references/api-reference.md
autonomous: true
requirements:
  - ONBOARD-07

must_haves:
  truths:
    - "references/api-reference.md contains no occurrences of the maintainer-specific domain coolify.cicd.streamlinity.com"
    - "references/api-reference.md contains no occurrences of the maintainer-specific Doppler account name streamlinity"
    - "references/api-reference.md contains no occurrences of the maintainer-specific SSH alias v_cicd_stream (matches ROADMAP SC #2 `cicd` pattern check)"
    - "The file opens with a one-line note instructing the reader to substitute <your-coolify-domain>, <your-doppler-account>, and <your-ssh-host> throughout"
    - "All Coolify URL examples use <your-coolify-domain> as the host placeholder; all doppler --account examples use <your-doppler-account>; all ssh examples use <your-ssh-host>"
  artifacts:
    - path: "references/api-reference.md"
      provides: "Domain-neutral Coolify + Doppler API reference"
      contains: "<your-coolify-domain>"
  key_links:
    - from: "references/api-reference.md top of file"
      to: "placeholder convention note"
      via: "blockquote at line 1-3 before any code examples"
      pattern: "<your-coolify-domain>"
---

<objective>
Genericize `references/api-reference.md` by replacing all maintainer-specific values (`coolify.cicd.streamlinity.com`, `skillmap-staging.streamlinity.com`, `streamlinity` Doppler account name, `v_cicd_stream` SSH alias) with placeholders. Add a one-line note at the top of the file explaining the placeholder convention so readers know to substitute their own values.

Purpose: The file is currently linked from SKILL.md as the API reference any new user might read while integrating. Maintainer-specific examples confuse readers who don't recognize that `streamlinity` is the maintainer's Doppler workspace, that `v_cicd_stream` is the maintainer's SSH host alias, and not required values. ROADMAP SC #2 (`grep -c 'streamlinity\|vultr-stream\|cicd' ...` must return 0) explicitly requires the `cicd` substring removal — and `v_cicd_stream` on line 228 currently matches that pattern.

Output: Updated `references/api-reference.md` with placeholders and a top-of-file substitution note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-new-user-onboarding/02.1-CONTEXT.md
@references/api-reference.md
</context>

<interfaces>
<!-- All maintainer-specific strings in references/api-reference.md as of phase start. -->
<!-- Output of: grep -n "streamlinity\|coolify.cicd\|v_cicd_stream" references/api-reference.md -->

```
  5: Base URL: `https://coolify.cicd.streamlinity.com/api/v1` (after SSL enabled + allowed_ips cleared)
 70:     "domains": "https://skillmap-staging.streamlinity.com",
 89:     "fqdn": "https://skillmap-staging.streamlinity.com",
190: doppler --account streamlinity configs tokens create "coolify-staging" \
204: doppler --account streamlinity secrets download \
218: doppler --account streamlinity secrets get \
228:   Use SSH to create Docker named volumes: `ssh v_cicd_stream "docker volume create <name>"`
```

Replacements (per D-09 + ROADMAP SC #2 — `cicd` substring must NOT remain anywhere in the file):
- `coolify.cicd.streamlinity.com` → `<your-coolify-domain>` (so the base URL becomes `https://<your-coolify-domain>/api/v1`)
- `skillmap-staging.streamlinity.com` → `skillmap-staging.<your-app-domain>` (keep `skillmap-staging.` prefix as illustrative example; substitute only the maintainer's apex domain)
- `--account streamlinity` → `--account <your-doppler-account>`
- `v_cicd_stream` → `<your-ssh-host>` (line 228 — matches ROADMAP SC #2 `cicd` pattern, MUST be replaced or SC #2 fails)

Note: after these replacements, neither `streamlinity` nor any string containing `cicd` should appear in the file.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add top-of-file placeholder note and replace all maintainer-specific values</name>
  <files>references/api-reference.md</files>
  <read_first>
    - references/api-reference.md (full file — must read current state to verify replacement count matches the 7 occurrences listed in the interfaces block, including line 228)
    - .planning/phases/02.1-new-user-onboarding/02.1-CONTEXT.md (D-09 — exact placeholder values and note wording)
    - .planning/ROADMAP.md (Phase 02.1 SC #2 — confirms `cicd` substring must be removed everywhere)
  </read_first>
  <action>
    Apply two coordinated edits.

    EDIT 1 — Insert a placeholder convention note at the very top of the file. The current file starts with:
    ```
    # Coolify + Doppler API Reference

    Source: `.planning/phases/07-coolify-doppler-deploy-infrastructure/07-RESEARCH.md`

    Base URL: `https://coolify.cicd.streamlinity.com/api/v1` (after SSL enabled + allowed_ips cleared)
    ```

    Insert a blockquote note immediately after the `# Coolify + Doppler API Reference` heading and before the `Source:` line, AND rewrite the base URL line:

    ```
    # Coolify + Doppler API Reference

    > Replace `<your-coolify-domain>` with your Coolify instance root URL (e.g. `coolify.example.com`), `<your-doppler-account>` with your Doppler account slug, and `<your-ssh-host>` with the SSH alias for your Coolify VPS (from `~/.ssh/config`) throughout this document. The examples use placeholder values; you must substitute your own.

    Source: `.planning/phases/07-coolify-doppler-deploy-infrastructure/07-RESEARCH.md`

    Base URL: `https://<your-coolify-domain>/api/v1` (after SSL enabled + allowed_ips cleared)
    ```

    EDIT 2 — Replace every remaining maintainer-specific value in the file. There are FOUR distinct replacement classes; apply each exhaustively (use a global string replace, not single-line edits):

    | Old | New | Where it appears |
    |-----|-----|------------------|
    | `coolify.cicd.streamlinity.com` | `<your-coolify-domain>` | line 5 (covered by EDIT 1) and anywhere else |
    | `skillmap-staging.streamlinity.com` | `skillmap-staging.<your-app-domain>` | lines 70, 89 (in JSON payloads) |
    | `--account streamlinity` | `--account <your-doppler-account>` | lines 190, 204, 218 (in `doppler` invocations) |
    | `v_cicd_stream` | `<your-ssh-host>` | line 228 (in the `## Notes` section, inside the `ssh ...` example) |

    Use exact-string replacement — do NOT touch the surrounding code (the example `curl` commands, JSON payloads, `doppler` invocations, and the `ssh` example in Notes should keep their structure; only the highlighted tokens change). The base URL line in the file header is already rewritten by EDIT 1.

    After both edits, the following invariants must hold:
    - The word `streamlinity` appears ZERO times in the file.
    - The substring `cicd` appears ZERO times in the file (this covers both `coolify.cicd` and `v_cicd_stream`).
    - The placeholders `<your-coolify-domain>`, `<your-doppler-account>`, `<your-app-domain>`, and `<your-ssh-host>` each appear at least once.
    - The top-of-file note appears before the first `## ` section.

    Do NOT modify any other content: keep all `curl` invocations, all JSON payloads, all `Response:` comments, all section headings, all numbered list ordering exactly as they are.
  </action>
  <verify>
    <automated>cd /home/cnut/development/claude-skills-deploy && ! grep -q 'streamlinity' references/api-reference.md && ! grep -q 'cicd' references/api-reference.md && grep -q '<your-coolify-domain>' references/api-reference.md && grep -q '<your-doppler-account>' references/api-reference.md && grep -q '<your-app-domain>' references/api-reference.md && grep -q '<your-ssh-host>' references/api-reference.md && grep -q '> Replace `<your-coolify-domain>`' references/api-reference.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'streamlinity' references/api-reference.md` (zero occurrences — the word does not appear anywhere)
    - `! grep -q 'cicd' references/api-reference.md` (zero occurrences — covers both `coolify.cicd.streamlinity.com` and `v_cicd_stream`, satisfies ROADMAP SC #2)
    - `grep -q '<your-coolify-domain>' references/api-reference.md` (placeholder present)
    - `grep -q '<your-doppler-account>' references/api-reference.md` (placeholder present)
    - `grep -q '<your-app-domain>' references/api-reference.md` (placeholder present in the per-app FQDN examples)
    - `grep -q '<your-ssh-host>' references/api-reference.md` (placeholder present in the Notes section ssh example)
    - `grep -q '> Replace `<your-coolify-domain>`' references/api-reference.md` (top-of-file note present as a blockquote)
    - The placeholder note appears BEFORE the first `## ` (H2) section (verifiable by line number: line of the note < line of first `^## ` match)
    - `grep -c 'Base URL: `https://<your-coolify-domain>/api/v1`' references/api-reference.md` returns 1 (the original base-URL line was rewritten, not duplicated)
    - File still ends with `## Notes` section content intact (the rewrite did not truncate the file): `grep -q '## Notes' references/api-reference.md`
  </acceptance_criteria>
  <done>references/api-reference.md is domain-neutral. A reader sees the substitution convention before the first example, and every `curl`, `doppler`, and `ssh` invocation uses placeholders instead of maintainer-specific values. ROADMAP SC #2 passes for this file (`cicd` substring removed).</done>
</task>

</tasks>

<verification>
- `! grep -q 'streamlinity' references/api-reference.md` (no matches)
- `! grep -q 'cicd' references/api-reference.md` (no matches — satisfies ROADMAP SC #2)
- File renders as valid markdown (no broken code fences — verify by counting backtick triples: `grep -c '^```' references/api-reference.md` returns an even number)
</verification>

<success_criteria>
- The API reference is usable by any reader without knowing what `streamlinity` is, what `v_cicd_stream` is, or why either appears
- The placeholder convention is documented inline at the top of the file so readers don't have to infer it from context
- The `cicd` substring no longer appears anywhere in the file, allowing ROADMAP Phase 02.1 SC #2 (`grep -c 'streamlinity\|vultr-stream\|cicd' test/e2e.sh SKILL.md references/api-reference.md` returns 0) to pass after this plan executes
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-new-user-onboarding/02.1-04-SUMMARY.md`
</output>

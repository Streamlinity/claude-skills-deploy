---
phase: 999.1-coolify-json-schema-enforcement
plan: 02
subsystem: infra
tags: [bash, doppler, coolify, seed, provision, gap-fill]

# Dependency graph
requires:
  - phase: 999.1-01
    provides: validate.sh gap-fill removal (--seed-from-env flag stripped; _parse_env_file and _fill_missing_from_env removed from validate.sh)
provides:
  - scripts/seed.sh — explicit Doppler gap-fill from .env.local and .env.production
  - provision.sh accepts 'provision' as first argument without error
  - provision.sh auto-calls seed.sh when .env files present after validate passes
  - SKILL.md subcommand table with provision + seed rows; validate described as strictly read-only
affects: [999.1-03, 999.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "seed.sh: gap-fill as an explicit operator action with a clear subcommand name, not a hidden validate side-effect"
    - "provision orchestrates: validate → seed (if .env files) → Coolify mutations"

key-files:
  created:
    - scripts/seed.sh
  modified:
    - scripts/provision.sh
    - SKILL.md

key-decisions:
  - "seed as explicit subcommand (not hidden in validate): gap-fill is an intentional data mutation that belongs in its own named command"
  - "provision alias is a no-op in provision.sh arg parser (case statement), not SKILL.md routing — co-locates all arg handling in the script"
  - "seed auto-called by provision.sh only when .env files present — zero-footprint for repos without local .env files"

patterns-established:
  - "Explicit over implicit: gap-fill is now opt-in via 'seed' subcommand, not a silent side-effect of validate"

requirements-completed: [SCHEMA-06, SCHEMA-07, SCHEMA-08]

# Metrics
duration: 3min
completed: 2026-06-17
---

# Phase 999.1 Plan 02: seed.sh + provision alias + SKILL.md update Summary

**scripts/seed.sh extracted from validate.sh gap-fill logic; provision.sh wired to auto-call seed when .env files present; SKILL.md subcommand table complete with seed, provision, and strictly-read-only validate descriptions**

## Performance

- **Duration:** 3 min
- **Started:** 2026-06-17T04:46:48Z
- **Completed:** 2026-06-17T04:49:34Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Created `scripts/seed.sh` with `_parse_env_file` and `_fill_missing_from_env` functions ported from the validate.sh gap-fill section removed in plan 01
- Added `provision)` no-op case to provision.sh arg parser so the command accepts an explicit subcommand alias
- Wired seed.sh auto-call in provision.sh after validate passes, guarded by `.env.local`/`.env.production` presence
- Updated SKILL.md subcommand table with `provision` and `seed` rows; rewrote `validate` row and validate flow section as strictly read-only; updated init_cicd note to reflect `doppler_token` as Tier 1 required

## Task Commits

Each task was committed atomically:

1. **Task 1: Create scripts/seed.sh** - `f154616` (feat)
2. **Task 2: Wire provision.sh + update SKILL.md** - `26605c4` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified
- `scripts/seed.sh` — new file; explicit Doppler gap-fill from .env files; seeds stg+dev from .env.local, prd from .env.production; never overwrites existing values
- `scripts/provision.sh` — added `provision)` alias case; added seed integration block after validate call
- `SKILL.md` — updated frontmatter description and argument-hint; added provision + seed rows to subcommand table; rewrote validate row; updated init_cicd gap-fill note; rewrote validate flow section

## Decisions Made
- `provision)` alias handled in provision.sh arg parser (not in SKILL.md routing) — co-locates all argument handling in the script; consistent with how `--plan` and `--rotate-tokens` are handled
- seed.sh sources both lib-coolify-api.sh and lib-doppler-api.sh to call `coolify_load_server` and `doppler_load_account` before seeding — matches the pattern established by validate.sh and provision.sh for server context loading

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## Next Phase Readiness
- seed.sh is ready for plan 03 (contract tests) — the `_fill_missing_from_env` function and exit behavior are testable with offline fixtures
- provision.sh seed integration is testable: pass a YAML_PATH pointing to a temp dir with a .env.local present, verify seed.sh is called
- SKILL.md routing for `seed` subcommand is complete — Claude will route `/setup-coolify seed` to `bash $HOME/.claude/skills/setup-coolify/scripts/seed.sh`

## Self-Check

Checking created/modified files and commits exist:

---
*Phase: 999.1-coolify-json-schema-enforcement*
*Completed: 2026-06-17*

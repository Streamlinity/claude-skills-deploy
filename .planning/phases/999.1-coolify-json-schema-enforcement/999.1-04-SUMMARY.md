---
phase: 999.1-coolify-json-schema-enforcement
plan: "04"
subsystem: testing
tags: [validate, contract-tests, e2e, offline, bash]

requires:
  - phase: 999.1-01
    provides: validate.sh Tier 1 doppler_token hard-fail + MSRV-07 deploy_ssh_host + hook support
  - phase: 999.1-02
    provides: seed.sh + provision alias (validate.sh is read-only after this)

provides:
  - test/validate-schema-contract.sh: 7 offline contract tests V1-V6+V3.5 for validate.sh schema enforcement
  - E2E hello-world deployment confirmed against vultr-stream (19/20 steps pass)
  - validate.sh hook exit propagation bug fixed (final ERRORS check added after hook section)

affects: [future test additions, CI regression coverage for validate.sh]

tech-stack:
  added: []
  patterns:
    - "Offline validate.sh contract tests: override HOME + COOLIFY_REGISTRY + PATH stubs (curl/ssh/doppler) for full isolation"
    - "Per-test HOME dir pattern: mktemp -d per test variant for hermetic fixture isolation"

key-files:
  created:
    - test/validate-schema-contract.sh
  modified:
    - scripts/validate.sh

key-decisions:
  - "Stub strategy: override HOME to control ~/.claude/coolify.json path + PATH prepend for curl/ssh/doppler stubs — fully isolates validate.sh from live services"
  - "validate.sh bug fix: add final ERRORS guard after hook section; hook fail() was silently ignored (exit 0 instead of exit 1)"
  - "E2E Coolify status glitch documented: production app served HTTP 200 smoke test but Coolify API reported exited:unhealthy during 30s retry window (transient propagation delay)"

patterns-established:
  - "Contract test file: one TMP dir, one bin/ dir with stubs, one per-test home dir via mktemp — keeps each test hermetic"
  - "curl stub: URL path match via case statement returning fixture JSON for /projects, /servers, /applications paths"
  - "doppler stub: handles secrets get (returns test-value), whoami, configure subcommands"

requirements-completed: [SCHEMA-12, SCHEMA-13, SCHEMA-14]

duration: 9min
completed: "2026-06-17"
---

# Phase 999.1 Plan 04: Contract Tests + E2E Run Summary

**Offline contract tests V1-V6+V3.5 for validate.sh schema enforcement, plus E2E hello-world deployment against vultr-stream with doppler_token added to coolify.json**

## Performance

- **Duration:** 9 min
- **Started:** 2026-06-17T06:07:11Z
- **Completed:** 2026-06-17T06:16:14Z
- **Tasks:** 2 (Task 1 committed; Task 2 no worktree file changes)
- **Files modified:** 3 (test/validate-schema-contract.sh created, scripts/validate.sh patched, ~/.claude/coolify.json updated)

## Accomplishments

- Created test/validate-schema-contract.sh: 7 fully offline tests covering V1 (doppler_token Tier 1), V2 (cloudflare Tier 2 cross-check), V3 (baseline exits 0), V3.5 (MSRV-07 deploy_ssh_host coupling), V4 (env_vars grep scan WARN), V5 (hook called), V6 (hook failure propagated) — all 15 assertions pass
- Fixed validate.sh: hook failure was silently ignored because the final ERRORS guard was before the hook section (not after). Added a final `if [ "$ERRORS" -gt 0 ]; then exit 1; fi` after the INV-01 block so V6 propagates correctly
- E2E hello-world deployment ran against vultr-stream: 19/20 steps passed; production smoke test returned HTTP 200; one step failure (`exited:unhealthy` from Coolify API) is a transient status propagation delay (app was serving correctly as verified by Step 11)
- Added `doppler_token` to vultr-stream entry in `~/.claude/coolify.json` (machine-local, not committed) so validate.sh Tier 1 check passes for E2E

## Task Commits

1. **Task 1: Create offline contract tests + fix validate.sh hook propagation** - `0841eb8` (test)
2. **Task 2: E2E compatibility verified + E2E run** - no commit (no file changes: e2e.sh had no --seed-from-env refs; test/results/ is gitignored)

## Files Created/Modified

- `test/validate-schema-contract.sh` — 7 offline tests V1-V6+V3.5 with curl/ssh/doppler stubs and per-test HOME isolation
- `scripts/validate.sh` — added final ERRORS check after hook section (line ~388) to propagate hook failure as exit 1
- `~/.claude/coolify.json` — added `doppler_token` to vultr-stream server entry (machine-local, never committed)

## Decisions Made

- **Stub strategy:** Override `HOME` env var per test so `$HOME/.claude/coolify.json` points to fixture (lib-config.py get-json-field uses `$HOME` directly). Prepend `$TMP/bin` to PATH for curl/ssh/doppler stubs. Set `COOLIFY_REGISTRY` for lib-coolify-api.sh/lib-doppler-api.sh. This fully isolates all validate.sh external calls.
- **validate.sh hook bug:** The existing code called `fail()` on hook failure but had no subsequent ERRORS check — the final `if [ "$ERRORS" -gt 0 ]; then exit 1; fi` was placed before the hook section, not after. Fixed by adding a second guard at the end of the script.
- **E2E Coolify glitch:** `exited:unhealthy` for production during 30s retry window is a known Coolify status propagation delay. The Step 11 HTTP smoke test returned 200 confirming the app was live. Post-run API check confirmed `running:healthy`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] validate.sh hook exit code not propagated**
- **Found during:** Task 1 (V6 contract test failure)
- **Issue:** `.coolify/validate.sh` hook failures called `fail()` (incrementing ERRORS) but the script's only `$ERRORS > 0` guard appeared before the hook section, so V6 (hook exits 1) caused exit 0 instead of exit 1
- **Fix:** Added final `if [ "$ERRORS" -gt 0 ]; then exit 1; fi` block after the INV-01 section and before the `echo "OK: ..."` lines in validate.sh
- **Files modified:** scripts/validate.sh
- **Verification:** V6 assertion `PASS: V6: exit code is non-zero (1) when hook exits 1` — all 15 test assertions pass
- **Committed in:** 0841eb8 (Task 1 commit)

**2. [Rule 3 - Blocking] Worktree was on stale branch without Phase 999.1 changes**
- **Found during:** Task 1 first run (validate.sh showed WARN not FAIL for doppler_token)
- **Issue:** Worktree `agent-aa43be2ed03d6b0ea` was at commit `ba589e4` (pre-999.1); main was at `aa14de9` with all Phase 999.1 plan 01/02 changes
- **Fix:** `git reset --hard main` in worktree after stashing test file; restored stash
- **Files modified:** None (worktree state correction)
- **Verification:** validate.sh line 66-74 confirmed Tier 1 doppler_token fail() check present after reset

---

**Total deviations:** 2 auto-fixed (1 bug, 1 blocking)
**Impact on plan:** Both fixes necessary for test correctness. No scope creep.

## Issues Encountered

- **Worktree branch mismatch:** Worktree was on an older branch without Phase 999.1 changes. Fixed by resetting to main before writing tests.
- **E2E Coolify production status glitch:** `exited:unhealthy` during retry window — transient Coolify API behavior, not a code failure. Production HTTP smoke test passed; post-run API confirmed `running:healthy`.

## Known Stubs

None. All contract tests use stubs intentionally for offline execution. The stubs are internal to test/validate-schema-contract.sh and are documented in the test header comment.

## Next Phase Readiness

- Phase 999.1 is complete: validate.sh has Tier 1/2 enforcement, contract tests cover all behaviors, E2E confirms full stack works
- test/validate-schema-contract.sh is ready for CI integration as a fast offline regression check
- The worktree can be merged into main via the orchestrator

---
*Phase: 999.1-coolify-json-schema-enforcement*
*Completed: 2026-06-17*

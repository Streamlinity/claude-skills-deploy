---
phase: 999.1-coolify-json-schema-enforcement
plan: 01
subsystem: infra
tags: [bash, validate, doppler, coolify, schema-enforcement]

# Dependency graph
requires: []
provides:
  - validate.sh with doppler_token as Tier 1 hard fail
  - validate.sh with no gap-fill/mutation side-effects (--seed-from-env removed)
  - validate.sh Tier 2 cross-check for cloudflare_api_token when dns.credential_source=coolify_json
  - validate.sh env_vars grep scan (WARN-only, .git-guarded)
  - validate.sh .coolify/validate.sh hook support with fail() propagation
affects: [999.1-02, 999.1-03, 999.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Tier 1/2 hard fail in validate.sh: critical fields fail immediately, optional fields warn"
    - "env_vars grep scan guarded by .git presence — skips E2E temp dirs"
    - "opt-in hook pattern: .coolify/validate.sh invoked if present, non-zero exit is FAIL"

key-files:
  created: []
  modified:
    - scripts/validate.sh

key-decisions:
  - "doppler_token promoted from advisory WARN to Tier 1 hard fail — prevents silent Doppler workspace mismatch"
  - "--seed-from-env removed from validate.sh entirely — validate must be guaranteed read-only"
  - "Gap-fill relocated to seed.sh (future plan) — explicit boundary between validate and seed"
  - "cloudflare_api_token cross-check added as Tier 2 — only fails when dns.credential_source: coolify_json"
  - "env_vars grep scan is WARN-only — stale env vars are informational, not blocking"

patterns-established:
  - "Tier 1 checks: hard fail early (before any API calls) for fields required for correct operation"
  - "Tier 2 checks: context-conditional hard fail (dns block, deploy_server coupling)"
  - "WARN-only scans: best-effort, never block validate — reserved for informational signals"

requirements-completed: [SCHEMA-01, SCHEMA-02, SCHEMA-03, SCHEMA-04, SCHEMA-05]

# Metrics
duration: 3min
completed: 2026-06-17
---

# Phase 999.1 Plan 01: validate.sh Schema Enforcement Summary

**validate.sh hardened: doppler_token is now a Tier 1 hard fail, --seed-from-env removed, Tier 2 cloudflare cross-check, env_vars grep scan, and .coolify/validate.sh hook support added**

## Performance

- **Duration:** 3 min
- **Started:** 2026-06-17T04:39:49Z
- **Completed:** 2026-06-17T04:42:57Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Removed `--seed-from-env` flag and all gap-fill code (_parse_env_file, _fill_missing_from_env) — validate.sh is now guaranteed read-only
- Added strict unknown-flag guard: any `--flag` now exits 1 with an actionable error
- Promoted `doppler_token` from advisory WARN to Tier 1 hard fail with early exit — prevents Doppler CLI targeting wrong workspace
- Added Tier 2 cross-check: cloudflare_api_token required in coolify.json when `dns.credential_source: coolify_json`
- Added env_vars grep scan (WARN-only, .git-guarded) to catch stale env var declarations
- Added `.coolify/validate.sh` hook support — non-zero exit propagated as FAIL

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove gap-fill side-effect and promote doppler_token to Tier 1** - `32ccec1` (feat)
2. **Task 2: Add Tier 2 cross-checks, env_vars grep scan, and hook support** - `28a1175` (feat)

## Files Created/Modified

- `scripts/validate.sh` — Tier 1/2/3 schema enforcement + env_vars grep scan + hook support, no gap-fill

## Decisions Made

- doppler_token promoted to Tier 1 with early exit because subsequent Doppler key checks are meaningless without knowing which workspace is targeted
- --seed-from-env removed entirely (not weakened) — the separation of validate (read-only) and seed (mutation) must be hard
- env_vars grep scan is WARN-only because a key missing from the codebase may be valid (server-side or framework-injected vars)
- Hook invocation uses `bash "$HOOK"` not `source` to avoid polluting validate.sh's variable scope

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- validate.sh is now a guaranteed read-only operation with Tier 1/2 schema enforcement
- 999.1-02 can implement the `seed` subcommand with confidence that validate has no mutation side-effects
- 999.1-03 and 999.1-04 can rely on doppler_token being enforced as Tier 1

---
*Phase: 999.1-coolify-json-schema-enforcement*
*Completed: 2026-06-17*

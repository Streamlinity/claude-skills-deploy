---
phase: 999.1-coolify-json-schema-enforcement
plan: 03
subsystem: docs
tags: [coolify.json, schema, doppler_token, tier-model, documentation]

requires:
  - phase: 999.1-coolify-json-schema-enforcement
    provides: research and context on schema enforcement requirements

provides:
  - examples/coolify.json.example with REPLACE_THIS placeholders for all 5 Tier 1 fields
  - docs/schema.md Field Tiers section with 3-tier model
  - doppler_token moved from Optional to Required Fields table in schema.md
  - cloudflare_api_token, deploy_ssh_host, deploy_vps_ip labeled as Tier 2 in schema.md
  - docs/setup-guide.md cleaned of obsolete auto-fill validate side-effect language

affects: [999.1-coolify-json-schema-enforcement, developer onboarding, validate.sh doc users]

tech-stack:
  added: []
  patterns:
    - "3-tier coolify.json field model: Tier 1 always required, Tier 2 feature-conditional, Tier 3 optional defaults"
    - "examples/coolify.json.example uses _key annotation pattern for inline documentation in JSON"

key-files:
  created:
    - examples/coolify.json.example
  modified:
    - docs/schema.md
    - docs/setup-guide.md

key-decisions:
  - "doppler_token promoted to Tier 1 required: prevents silent cross-workspace targeting in multi-workspace setups"
  - "_tier*_note annotation keys in coolify.json.example document tier ownership without needing comments in JSON"
  - "Field Tiers section placed before Required Fields table for logical reading order"
  - "cloudflare_api_token and deploy_ssh_host/deploy_vps_ip labeled Tier 2 in Optional table to distinguish conditional-required from truly optional"

patterns-established:
  - "Tier annotation pattern: describe Tier 2 fields as 'Tier 2 — required when <condition>' in Optional table"

requirements-completed: [SCHEMA-09, SCHEMA-10, SCHEMA-11]

duration: 4min
completed: 2026-06-17
---

# Phase 999.1 Plan 03: Schema Documentation Summary

**3-tier coolify.json field model documented: examples/coolify.json.example created with REPLACE_THIS placeholders, doppler_token moved to Required, Tier 2 fields labeled in Optional table**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-17T04:39:53Z
- **Completed:** 2026-06-17T04:43:00Z
- **Tasks:** 2
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments

- Created `examples/coolify.json.example` — valid JSON with 5 REPLACE_THIS Tier 1 fields, annotated Tier 2 block, annotated Tier 3 defaults using `_key` annotation convention
- Updated `docs/schema.md` — added Field Tiers section with 3-tier table, moved `doppler_token` from Optional to Required table as 5th Tier 1 field, labeled `cloudflare_api_token` and deploy fields as Tier 2
- Cleaned `docs/setup-guide.md` — removed obsolete auto-fills comment from validate step that described `--seed-from-env` side-effects no longer present

## Task Commits

1. **Task 1: Create examples/coolify.json.example** - `740e44a` (docs)
2. **Task 2: Update docs/schema.md + docs/setup-guide.md** - `8ec4c48` (docs)

## Files Created/Modified

- `examples/coolify.json.example` - Copy-and-fill-in reference for new server entries; JSON with `_doc`, `_tier*_note`, and `_*_when` annotation keys
- `docs/schema.md` - Added Field Tiers section (before Required Fields), moved doppler_token to Required table, labeled Tier 2 fields in Optional table
- `docs/setup-guide.md` - Replaced 4-line auto-fill comment at validate step with accurate 2-line description

## Decisions Made

- `doppler_token` is now Tier 1 required: it prevents the Doppler CLI from silently targeting the wrong workspace in multi-org setups. Previously marked optional but effectively required for correctness.
- JSON annotation pattern uses `_tier1_required_note`, `_tier2_note`, etc. keys rather than a separate `.md` README, keeping the example self-contained.
- Field Tiers section placed immediately before Required Fields table so readers encounter the mental model before the field list.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Plan's Python verification script was overly broad for doppler_token placement check**
- **Found during:** Task 2 verification
- **Issue:** The plan's Python check used a simple `in_required` / `in_optional` flag that did not terminate the "optional zone" at section separators. The JSON example block in the "Complete Annotated Example" section (line 294) contains `doppler_token`, which triggered `found_in_optional = True` as a false positive.
- **Fix:** Verified correctness using a more precise Python check that terminates the optional zone at `---` separators or new `##` headings. The actual Optional Fields table does NOT contain `doppler_token` — the check passed with precise boundaries.
- **Files modified:** None (verification script adjustment only; file state is correct)
- **Verification:** Precise Python check confirmed: `required: True optional: False`

---

**Total deviations:** 1 (false-positive in verification script — no file changes needed)
**Impact on plan:** No scope creep. Files are correct; the plan verification script needed tighter scope boundaries.

## Issues Encountered

None beyond the verification script false positive documented above.

## Next Phase Readiness

- Plan 999.1-04 can proceed — docs/schema.md, docs/setup-guide.md, and examples/coolify.json.example are consistent with the enforced tier model
- validate.sh (Plan 999.1-01/02) changes for doppler_token enforcement are documented in schema.md

---
*Phase: 999.1-coolify-json-schema-enforcement*
*Completed: 2026-06-17*

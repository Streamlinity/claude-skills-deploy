---
phase: 999.1-coolify-json-schema-enforcement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/coolify.json.example
  - docs/schema.md
  - docs/setup-guide.md
autonomous: true
requirements: [SCHEMA-09, SCHEMA-10, SCHEMA-11]

must_haves:
  truths:
    - "examples/coolify.json.example exists with REPLACE_THIS placeholders for all Tier 1 fields including doppler_token"
    - "docs/schema.md has a 3-tier model section and doppler_token in the Required Fields table"
    - "docs/schema.md does not list doppler_token in the Optional Fields table"
    - "docs/setup-guide.md has no references to --seed-from-env or validate side-effects"
  artifacts:
    - path: "examples/coolify.json.example"
      provides: "Copy-and-fill-in reference for new server entries"
      contains: "REPLACE_THIS.*doppler_token\|doppler_token.*REPLACE_THIS"
    - path: "docs/schema.md"
      provides: "Field Tiers section + updated Required/Optional tables"
      contains: "Tier 1\|Tier 2\|Tier 3"
  key_links:
    - from: "docs/schema.md"
      to: "examples/coolify.json.example"
      via: "field tier reference"
      pattern: "Tier 1.*required\|required.*Tier 1"
---

<objective>
Create examples/coolify.json.example with REPLACE_THIS placeholders. Update docs/schema.md to add a 3-tier model section and move doppler_token to Required Fields. Update docs/setup-guide.md to remove --seed-from-env references.

Purpose: developers copying coolify.json.example should see exactly which fields are required and which are optional. schema.md must reflect the enforced tier model. setup-guide.md must not describe obsolete validate behavior.

Output: examples/coolify.json.example (new), docs/schema.md (updated tier model), docs/setup-guide.md (minor cleanup).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-coolify-json-schema-enforcement/999.1-CONTEXT.md
@.planning/phases/999.1-coolify-json-schema-enforcement/999.1-RESEARCH.md

<interfaces>
<!-- Executor must read these before editing -->

From docs/schema.md (current structure):
- Lines 153-165: Required Fields table (url, api_key, doppler_account, ssh_host — 4 fields)
- Lines 167-179: Optional Fields table (doppler_token, server_name, vps_ip, cloudflare_api_token, dns_default, deploy_ssh_host, deploy_vps_ip)
- Line 179: "Keeping server entries up to date" note (references WARN: for optional fields — must be updated)

From examples/skillmap/coolify.yaml:
  Style reference — plain YAML, no annotation comments (coolify.json.example will have inline comments using JSON comments workaround: double-slash lines are NOT valid JSON; use a "// comment" field convention only in .example files, not runtime files. Actually: since JSON has no comments, use a key like "_tier1_required": "..." as a label? No — the CONTEXT.md says pattern matches examples/skillmap/coolify.yaml style. coolify.yaml uses plain YAML. For coolify.json.example, use a simple structure with REPLACE_THIS placeholder strings as values and a descriptive README-style header comment inside the JSON using a "_doc" field — or just write a clean JSON file with REPLACE_THIS values.)

Correct approach for coolify.json.example: JSON has no comment syntax. Use a top-level comment as a "_note" key or as a header comment in a separate .md file. The RESEARCH.md says "examples/coolify.json.example should be immediately usable as a starting point (copy, fill in REPLACE_THIS values)". Keep it as clean JSON with REPLACE_THIS string values and a README note at the top as a "_doc" string field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create examples/coolify.json.example</name>
  <files>examples/coolify.json.example</files>
  <read_first>
    - docs/schema.md (lines 138-179 — full coolify.json section for accurate field descriptions)
    - examples/skillmap/coolify.yaml (style reference — use same clear structure)
  </read_first>
  <action>
Create examples/coolify.json.example. JSON has no comment syntax; use a "_doc" top-level key for the header note. Tier 2 fields go in a nested "_tier2_optional_features" object; Tier 3 in a "_tier3_optional_defaults" object. The real runtime JSON uses flat structure under "servers"; the example uses a commented block layout to make the tiers scannable.

Write the file with this structure and content:

```json
{
  "_doc": "Copy this file to ~/.claude/coolify.json and fill in REPLACE_THIS values. Fields marked REPLACE_THIS are Tier 1 required — validate.sh will hard-fail if any are absent. Remove this _doc key before use.",

  "servers": {
    "my-server": {
      "_tier1_required_note": "All five fields below are always required. validate.sh exits 1 if any are missing.",

      "url": "REPLACE_THIS_https://coolify.example.com",
      "api_key": "REPLACE_THIS_coolify_api_key",
      "doppler_account": "REPLACE_THIS_doppler_account_slug",
      "ssh_host": "REPLACE_THIS_ssh_alias_from_ssh_config",
      "doppler_token": "REPLACE_THIS_doppler_service_or_personal_token",

      "_tier2_note": "Tier 2 fields below are required only when specific coolify.yaml features are enabled. Remove any you do not need.",

      "_cloudflare_api_token_when": "ONLY if dns.credential_source: coolify_json in coolify.yaml",
      "cloudflare_api_token": "OPTIONAL_cf_api_token_zone_dns_edit_scope",

      "_deploy_ssh_host_when": "ONLY if deploy_server is set in coolify.yaml (multi-server deployments)",
      "deploy_ssh_host": "OPTIONAL_ssh_alias_for_app_vps",

      "_deploy_vps_ip_when": "ONLY if deploy_server is set AND dns automation is enabled",
      "deploy_vps_ip": "OPTIONAL_1_2_3_4",

      "_tier3_note": "Tier 3 fields below are truly optional. Defaults shown. Remove any you do not need.",

      "_server_name_default": "localhost (correct for standard Coolify single-host setup)",
      "server_name": "localhost",

      "_vps_ip_default": "resolved via SSH ifconfig.me when absent",
      "vps_ip": "OPTIONAL_1_2_3_4",

      "_dns_default_note": "Used by test/e2e.sh to inject a dns: block into E2E test runs",
      "dns_default": {
        "provider": "cloudflare",
        "zone_name": "OPTIONAL_example.com",
        "credential_source": "coolify_json",
        "credential_key": "cloudflare_api_token"
      }
    }
  }
}
```

Note: the `_doc`, `_tier1_required_note`, `_tier2_note`, `_cloudflare_api_token_when`, `_deploy_ssh_host_when`, `_deploy_vps_ip_when`, `_tier3_note`, `_server_name_default`, `_vps_ip_default`, `_dns_default_note` keys are annotation-only fields — they must be removed from the actual `~/.claude/coolify.json` file. This is explained in the `_doc` field.
  </action>
  <verify>
    <automated>
      cd /home/cnut/development/claude-skills-deploy &&
      python3 -c "import json; d=json.load(open('examples/coolify.json.example')); print('valid JSON')" &&
      grep -c "REPLACE_THIS" examples/coolify.json.example &&
      grep -c "doppler_token" examples/coolify.json.example
    </automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; json.load(open('examples/coolify.json.example'))"` exits 0 (valid JSON)
    - `grep -c "REPLACE_THIS" examples/coolify.json.example` returns at least 4 (url, api_key, doppler_account, ssh_host, doppler_token)
    - `grep -c "doppler_token" examples/coolify.json.example` returns at least 1 and the value contains REPLACE_THIS
    - `grep -c "ONLY if dns.credential_source" examples/coolify.json.example` returns at least 1 (Tier 2 annotation for cloudflare_api_token)
    - `grep -c "deploy_ssh_host\|deploy_vps_ip" examples/coolify.json.example` returns at least 2 (Tier 2 deploy fields present)
  </acceptance_criteria>
  <done>examples/coolify.json.example is valid JSON with REPLACE_THIS for all 5 Tier 1 fields, annotated Tier 2 blocks, annotated Tier 3 defaults</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/schema.md (3-tier model section + doppler_token to Required)</name>
  <files>docs/schema.md, docs/setup-guide.md</files>
  <read_first>
    - docs/schema.md (full file — understand all sections before editing)
    - docs/setup-guide.md (full file — grep for seed-from-env and validate side-effect language)
  </read_first>
  <action>
**docs/schema.md changes:**

1. **Move doppler_token from Optional to Required table:**
   - In the "Required Fields per Server Entry" table (currently 4 rows), add a 5th row:
     | `doppler_token` | Doppler personal or service token for the workspace associated with this server. Exported as `DOPPLER_TOKEN` before every Doppler CLI call, scoping operations to the correct workspace and preventing silent targeting of the wrong workspace in multi-workspace setups. Obtain from Doppler → Settings → Service Tokens (scoped service token) or Doppler → Profile → Personal Tokens. | `dp.st.xxxxx...` |
   - Remove the `doppler_token` row from the Optional Fields table.
   - Update the "Keeping server entries up to date" note at the end of the Optional section to remove the implication that `doppler_token` might be absent: change "any missing optional fields" to "any missing Tier 2 or Tier 3 fields".

2. **Add Field Tiers section** immediately before the "Required Fields per Server Entry" heading:

   ```markdown
   ### Field Tiers

   coolify.json server fields are organized into three tiers:

   | Tier | When required | validate.sh behavior |
   |------|---------------|----------------------|
   | **Tier 1** | Always — for every server entry | Hard fail (`FAIL:` line, non-zero exit) |
   | **Tier 2** | When a specific feature is enabled in `coolify.yaml` | Hard fail only when the feature is active |
   | **Tier 3** | Never strictly required — sensible defaults exist | No warning |

   **Tier 1 fields** (all required): `url`, `api_key`, `doppler_account`, `ssh_host`, `doppler_token`

   **Tier 2 fields** (required when feature is on):
   - `cloudflare_api_token` — required when `dns.credential_source: coolify_json` in `coolify.yaml`
   - `deploy_ssh_host` — required when `deploy_server:` is set in `coolify.yaml`
   - `deploy_vps_ip` — required when `deploy_server:` is set AND dns automation is enabled

   **Tier 3 fields** (optional, working defaults): `server_name`, `vps_ip`, `dns_default`

   See `examples/coolify.json.example` for a copy-and-fill-in reference with annotations.
   ```

3. **Update Optional Fields table description** for cloudflare_api_token to say "Tier 2 — required when dns.credential_source: coolify_json" instead of just "Only required when...".

**docs/setup-guide.md changes:**
1. Read the full file, then grep for `seed-from-env`, `gap-fill`, `--seed`, `validate.*fill`, `fill.*validate`.
2. For any occurrence: replace with a note that gap-filling is now done via `/setup-coolify seed` (the explicit subcommand), not via validate.
3. If no occurrences exist, no change needed — note this in the plan output.
  </action>
  <verify>
    <automated>
      cd /home/cnut/development/claude-skills-deploy &&
      grep -c "Tier 1\|Tier 2\|Tier 3" docs/schema.md &&
      grep -c "doppler_token" docs/schema.md &&
      python3 -c "
lines = open('docs/schema.md').readlines()
in_required = False
found_in_required = False
found_in_optional = False
for i, l in enumerate(lines):
    if 'Required Fields per Server Entry' in l:
        in_required = True
    if in_required and 'Optional Fields per Server Entry' in l:
        in_required = False
    if '| \`doppler_token\`' in l or 'doppler_token' in l:
        if in_required:
            found_in_required = True
        else:
            found_in_optional = True
print('required:', found_in_required, 'optional:', found_in_optional)
assert found_in_required, 'doppler_token not in Required section'
assert not found_in_optional, 'doppler_token still in Optional section'
print('OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Tier 1" docs/schema.md` returns at least 1
    - `grep -c "Field Tiers" docs/schema.md` returns at least 1
    - The Python check above exits 0 (doppler_token in Required section, not in Optional section)
    - `grep -c "seed-from-env\|--seed-from-env" docs/setup-guide.md` returns 0 (obsolete flag removed)
    - `grep -c "Tier 2.*cloudflare_api_token\|cloudflare_api_token.*Tier 2" docs/schema.md` returns at least 1
  </acceptance_criteria>
  <done>docs/schema.md has Field Tiers section, doppler_token in Required table, cloudflare_api_token labeled Tier 2; docs/setup-guide.md has no --seed-from-env references</done>
</task>

</tasks>

<verification>
Run from repo root:
```bash
cd /home/cnut/development/claude-skills-deploy
python3 -c "import json; json.load(open('examples/coolify.json.example')); print('valid JSON')"
grep -c "REPLACE_THIS" examples/coolify.json.example
grep -c "Tier 1" docs/schema.md
grep -c "seed-from-env" docs/setup-guide.md
# must be 0
```
</verification>

<success_criteria>
- examples/coolify.json.example: valid JSON, 5 REPLACE_THIS values (Tier 1), annotated Tier 2 block, annotated Tier 3 block
- docs/schema.md: Field Tiers section present, doppler_token in Required table, not in Optional table
- docs/schema.md: cloudflare_api_token and deploy_ssh_host labeled as Tier 2 conditionally required
- docs/setup-guide.md: no --seed-from-env references
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-coolify-json-schema-enforcement/999.1-03-SUMMARY.md`
</output>
